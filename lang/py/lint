#!/usr/bin/env bash

set -e # Exit on error
set -o xtrace

pylint --help
find . -name '*.py' -not -path './third_party/*' | xargs \
  pylint \
    --symbols y \
    --reports n \
    --indent-string='  ' \
    --disable=missing-docstring
